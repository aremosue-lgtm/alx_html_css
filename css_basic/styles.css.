/* --- Global Styling --- */
body {
    font-family: Arial, sans-serif;
    background-color: #fafafa;
    margin: 0;
    padding: 0;
}

/* --- Header Styling (non-positioning allowed) --- */
header {
    background-color: #333;
    padding: 10px 20px;
}

header ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 20px;
}

header a {
    color: white;
    text-decoration: none;
    font-weight: bold;
}

header a:hover {
    text-decoration: underline;
}

/* Logo styling (allowed, pre-defined class) */
.logo {
    font-size: 30px;
    color: gold;
}

/* --- Article Styling (you are free here!) --- */
article {
    background: white;
    padding: 20px;
    border-radius: 6px;
    margin: 20px;
}

article h1, article h2, article h3 {
    color: #333;
}

article p {
    line-height: 1.6;
}

/* Table styling */
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

table th, table td {
    padding: 10px;
    border: 1px solid #ccc;
}

table th {
    background-color: #eee;
}

/* Footer */
footer {
    text-align: center;
    padding: 20px;
    background-color: #333;
    color: white;
    margin-top: 20px;
}
